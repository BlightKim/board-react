import React from 'react';

const BoardMain = () => {
    return (
        <div style={{height: '100vh', backgroundColor: '#ff0000'}}>
            BoardMain
        </div>
    );
};

export default BoardMain;
